# PostgreSQL connection (change your password)
DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/live_adu"

# Directory where HLS segments are stored
HLS_OUTPUT_DIR="./public/hls"

# FFmpeg path (if not in PATH, provide the full path)
FFMPEG_PATH="ffmpeg"

# Node environment
NODE_ENV="development"
